# Always use the public registry explicitly (avoids slow mirror fallback)
registry=https://registry.npmjs.org/

# Use a local cache aggressively (can help in CI/local repeated installs)
prefer-offline=true
